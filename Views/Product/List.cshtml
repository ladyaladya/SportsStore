@model ProductListViewModel

@foreach (var prod in Model.Products)
{
    @Html.Partial("ProductSummary", prod);
}

<div page-model="@Model.PagingInfo" page-action="List" page-classes-enabled="true" page-class="btn"
     page-class-normal="btn-secondary" page-class-selected="btn-primary" class="btn-group pull-rigth m-1"></div>